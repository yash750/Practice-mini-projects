import { ChatPage } from "./ChatPage";

// BACKEND INTEGRATION COMMENT:
// This component is a wrapper for PDF-specific chat functionality
// It inherits all backend integration needs from ChatPage

export const PdfChat = () => {
  return <ChatPage />;
};